# 网页脚本 1.0.19：Gemini 后台网络正文优先

配套 XPI 0.8.9，也兼容回滚 XPI 0.8.8。完整更新与验收见 [0.8.9 发布说明](v0.8.9.md)。

正文来源与结束判断不再被后台滞后的 DOM 锁住：有效网络正文直接回传，结束帧与关闭传输经过独立静默计时确认完成；页面读取作后备。回传重试也使用独立计时，不重复发送用户问题，不抢前台焦点。新请求、取消及切换任务会清理旧计时器，防止串入下一问。

本次新增后台停绘制与回调停顿回归，但无法突破浏览器完整冻结/丢弃标签页。更新原脚本并刷新网页即可，不必清空原会话。
