# 当前进度

## 2026-09-06：0.5.2 阅读提示词与上下文优化（当前）

- 基于用户当前 0.5.1，按 surgical-patch 技能把修改集中在任务指引、材料取材、历史组装及相关测试；保留网页连接、API 设置、界面和现有存档。
- 新增分任务阅读指引；逐段翻译保留术语、数字、单位和公式；问答优先解决具体问题，方法解释补充变量、假设与适用范围。
- 当前页不再默默裁成 6,000 字符；全文概览按实际提取量提供完整文字或有范围标识的跨页摘录，关键词无命中时提供背景概览回退。
- API 本轮问题只发送一次，前轮原文随完整问答对保留；字符预算不足时成对裁剪并说明范围。
- 知识沉淀合并同类问题、保留后续纠正、区分已解决与待核实，不递归总结旧沉淀文档。
- 独立只读复核后补齐材料范围持久化和参考材料边界；新测试验证恢复会话仍保留回退、截断说明，历史 PDF 记录不会冒充当前附件。
- 前端测试 87 项通过；Python 79 项通过、2 项按环境跳过；ruff、mypy 和 JavaScript 语法检查通过。未进行真实模型回答质量 A/B 评测。
- 已构建 `dist/zotero-research-0.5.2.xpi` 及 SHA-256、打包清单；本机已启用插件仍为 0.5.1，本轮没有替换运行中的插件。
- 0.5.2 无需更新现有 1.0.2 油猴脚本。设计与限制见 `docs/PROMPT_DESIGN.md`。

## 2026-09-05：0.4.2 连接修复

- 以用户重构后的原生 Zotero relay 为基础，保留全部现有工作树变更；没有恢复 Python bridge 或旧配对方案。
- 修复油猴缺少 Zotero Connector 协议请求头导致的浏览器连接被提前断开；脚本升级至 1.0.1。
- 修复标签页接管后遗留长轮询、任务重复领取、断开后任务悬挂、迟到握手造成假连接。
- 修复侧栏异步取证阶段重复发送，以及切换文献/清空/销毁后的旧问题入队。
- 修复 PDFWorker.getPages 不存在的问题，使用本机 Zotero 10.0.1 实际提供的 getFullText(itemID)，保留物理页码。
- 0.4.2 XPI 已通过 Zotero 插件管理器安装并启用；安装文件与构建产物 SHA-256 一致。
- 47 项 JS 测试、78 项 Python 测试通过，2 项环境条件测试跳过；ruff、mypy 通过。
- 仍待用户更新浏览器油猴脚本并刷新网页。扩展管理页受浏览器工具限制，未代操作；真实网页发送与回答回传尚未验收。
- 详细审查结果：`docs/REVIEW_CONNECTION_2026-09-05.md`。

## 历史记录（以下为旧版 0.3，不代表当前架构）

## 当前状态

精简版功能已完成，代码版本为 0.3.0。XPI 已同步到当前 Zotero profile；当前剩余动作是重启 Zotero，让它重新加载插件并完成一次网页配对。

## 已完成

- 侧栏已收缩为论文标题、连接状态、快捷命令、选文卡片和网页 AI 连续聊天。
- 浏览器端 Tampermonkey 脚本支持 Gemini、DeepSeek 和 Google AI Studio。
- 配对参数使用 URL 片段传递，网页显示“已配对，等待消息”，随后自动清除地址栏片段。
- 会话消息通过本机 bridge 排队，网页脚本负责填入输入框、点击发送、读取回答并回传。
- Zotero 端只读取 Local API 和 PDF，不直接操作数据库。
- 功能范围已收敛为公开论文、PDF 选文和网页 AI 对话。
- 修复设置页等待本机后端路径的问题，改为静态连接提示。
- 独立的 Zotero MCP 注册已移除，插件只通过自己的 bridge 运行。
- 生成 `dist/zotero-research-0.3.0.xpi`、SHA-256 和打包清单。
- 网页 AI 连接器兼容性修复至用户脚本 `0.2.2`：等待 Gemini 发送控件可用，兼容新版 ARIA/Material 控件，并排除中继状态按钮误点击。
- 中文概括问题未命中英文关键词时，网页 AI 自动收到论文开头和结尾的有限上下文。

## 验证结果

- `npm run test:js`：31 项通过。
- `python -m pytest -q`：全部通过，2 项按环境跳过。
- `python -m ruff check src addon scripts tests`：通过。
- `python -m mypy`：17 个源文件无错误。

## 安装后的手动检查

1. 完全退出并重新打开 Zotero，打开一篇带 PDF 的论文。
2. 打开右侧“科研助手”面板，确认快捷命令和输入框出现。
3. 在 Tampermonkey 中更新 `userscripts/zotero-research-webai.user.js` 至 `0.2.2`，刷新网页 AI 页面。
4. 浏览器打开对应网页，在侧栏点击“配对/打开网页”。
4. 确认网页右下角显示“Zotero：已配对，等待消息”，再发送一条测试问题。
