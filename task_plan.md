# Zotero 10 科研阅读工作流完整实现

## Goal

在现有只读 MCP 基础上交付 Zotero 原生侧边栏、证据定位、确认式笔记/高亮写入、真实隐私边界、按需本地重解析和引用核验，并用可复现测试与安装包验收。

## Next Step

向用户交付 0.2 XPI、中文指南和验收记录；等待用户确认主 profile 安装及本地模型/权重准备。
不继续添加功能，不代替用户确认原生写入或云端内容授权。

## Current Phase

工程实现与隔离验收已完成；现场安装和模型验收待用户操作。

## Phases

### Phase 1: 环境和接口核验

- [x] 核实 Zotero 10.0.1 / Local API v3 / 支持写入。
- [x] 阅读上一版规格，确认没有用户未提交改动。
- [x] 参考页面不可访问已记录；直接核实本机 Zotero 10 源码和官方开发依据。
- [x] 模型/解析器现状已核实：Ollama 无模型，MinerU 未配置；下载待用户许可，隔离运行验收留至 Phase 4。
- [x] 固定验收边界与前后端契约。
- **Status:** complete

### Phase 2: 隐私、解析和证据后端

- [x] MCP 云端调用默认阻止敏感全文/笔记出站，明确公开文献的逐次授权。
- [x] 原生侧边栏经受保护的本机桥接调用共用服务。
- [x] 按需本地重解析、可追溯的 PDF 坐标与拒绝模糊匹配；无权重时明确失败。
- [x] 结构化精读、解释/问答、模拟审稿与引用核验；无模型时明确只有证据摘录。
- **Status:** complete

### Phase 3: Zotero 原生界面和写入

- [x] 可安装的 Zotero 10 扩展与生命周期清理。
- [x] 阅读器选文、侧边栏输入、处理状态与设置。
- [x] 证据点击定位和原生高亮预览/人工确认。
- [x] 笔记精确预览/人工确认及重复提交防护。
- **Status:** complete（功能已实现；完整手动点击验收见后续项）

### Phase 4: 集成与真实运行验收

- [x] 安全边界、外部协议及端到端行为自动化测试：154 Python + 34 JS；另有真实 Crossref 1 项通过。
- [x] 严格类型检查、代码规范、依赖和安装包校验；两次构建 SHA-256 相同。
- [x] 使用合成文献/隔离 Zotero 配置测试，真实原生高亮写入一次，笔记数保持 0。
- [x] 主库只读健康检查；不读真实论文、不写主库。
- **Status:** complete（自动化与隔离 API 运行范围）

### Phase 5: 审查和交付

- [x] 独立 Standards / Spec 双轴审查，8 项明确问题已修复并验证。
- [x] 更新中文安装/使用/隐私说明及实际能力矩阵。
- [x] 生成 XPI、启动方式和验收记录；逐项列明尚需用户操作的边界。
- **Status:** complete

### 后续现场验收（需要用户参与）

- [ ] 主 profile 手动安装 XPI 并确认。
- [ ] 完整侧边栏/快捷键/选文点击与视觉检查；窗口自动化当前报错，未冒称通过。
- [ ] 用户核对一份具体笔记，批准 Zotero 原生授权后保存。
- [ ] 用户批准下载本地模型，或提供现有模型端点；随后实际跑模型分析。
- [ ] 准备 MinerU 环境与完整权重后，实际验证复杂公式/扫描件。
- **Status:** waiting_for_user（不是代码测试失败；不虚构完成）

## Public test seams

- 已有规格约定的 MCP 工具、Local API HTTP、PDF 提取和模型接口。
- 新增原生侧边栏操作、带坐标证据、人工确认写入和云端出站闸门；已用异步问题征求用户确认。
- 测试只使用合成文献或明确公开且获准处理的材料。

## Decisions Made

| Decision | Rationale |
| --- | --- |
| 沿用官方 Local API 与 Zotero 插件 JS runtime，禁止直接读写 sqlite | 安全边界与既定设计一致 |
| 本地界面不经云端 Codex转发全文 | 修复上一版把本地解析误认为离线的缺口 |
| 工程新增功能必须有真实实现与可验证失败状态 | 不用占位接口冒充完整功能 |
| 子代理写入使用独立 worktree，主线程集成 | 避免覆盖彼此改动 |

## Errors Encountered

| Error | Attempt | Resolution |
| --- | --- | --- |
| 技能合并读取输出截断 | 1 | 分次读取，补齐缺失段落后再执行 |
| uv 重装当前正在运行的 MCP exe 遇到 Windows 文件锁 | 1 | 开发测试直接用 Python -m；新版本使用独立环境，不中断旧进程 |
| Feishu web抓取拒绝，浏览器读取超时 | 2 | 暂不重复重试，按用户已粘贴的明确设计推进 |
| 私有桥接Windows拒绝请求时发生RST | 1 | 对受限长度请求体有界丢弃后返回错误，环回HTTP测试通过 |
| Zotero 10 拒绝缺少 update_url 的 XPI | 1 | 核实本机 manifest 校验；补齐保留域名字段，明确手动升级 |
| jar 配置资源被旧 API 当作 XHR 返回 | 1 | 改为 getResourceAsync；bootstrap/设置页回归通过 |
| 空测试库创建条目早于加载导致 Reader/API 等待 | A/B 对照 | 先 await library.waitForDataLoad；有/无扩展两组均恢复 |
| 窗口控制返回 foreground window did not report a process id | 已按规则重试 | 停止盲目 UI 操作；API 集成通过，现场点击单独标未验证 |

## Non-goals

- 不复制商业插件专有代码、不绕过付费授权。
- 不升级/删除其他插件、不批量更改文献、不暴露本机端口到外网。
- 不擅自使用用户消息里的云端 API 密钥或上传真实未发表资料。
